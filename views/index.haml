%section.search
  %form#search{ action: "/whois/", method: "get" }
    %input{ type: "text", name: "name", value: params[:name] }
    %input{ type: "submit", value: "Search" }
  

%ul#results    
  - unless @results.nil?
    - @results.each do |it|
      = partial(:_whois, it)